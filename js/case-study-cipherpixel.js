window.addEventListener('scroll',()=>{const p=(window.scrollY/(document.documentElement.scrollHeight-window.innerHeight))*100;document.getElementById('read-progress').style.width=p+'%';});
const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');obs.unobserve(e.target);}}),{threshold:0.1,rootMargin:'0px 0px -60px 0px'});
document.querySelectorAll('[data-reveal]').forEach(el=>obs.observe(el));
(function(){const canvas=document.getElementById('hero-canvas');if(!canvas)return;const ctx=canvas.getContext('2d');let W,H,pixels=[];function resize(){W=canvas.width=canvas.offsetWidth;H=canvas.height=canvas.offsetHeight;}window.addEventListener('resize',resize);resize();for(let i=0;i<200;i++)pixels.push({x:Math.random()*1920,y:Math.random()*1080,size:Math.floor(Math.random()*3+1)*2,a:Math.random()*0.4+0.05,vx:(Math.random()-0.5)*0.4,vy:(Math.random()-0.5)*0.4,color:Math.random()>0.7?`rgba(58,125,232,`:`rgba(42,191,179,`});function draw(){ctx.clearRect(0,0,W,H);pixels.forEach(p=>{ctx.fillStyle=p.color+p.a+')';ctx.fillRect(p.x,p.y,p.size,p.size);p.x+=p.vx;p.y+=p.vy;if(p.x<0)p.x=W;if(p.x>W)p.x=0;if(p.y<0)p.y=H;if(p.y>H)p.y=0;});requestAnimationFrame(draw);}draw();})();